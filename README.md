
---

# 🚀 Linear Flow: Interactive LMS Platform

**Linear Flow** — это инновационная платформа для онлайн-обучения (LMS), построенная на принципе «диалогового обучения». В отличие от классических платформ, здесь фокус смещен с пассивного просмотра видео на активное взаимодействие с преподавателем через интегрированную систему чата и линейный контроль прогресса.

---

## 📖 Концепция проекта

Основная идея — **обучение как непрерывный поток общения**. 
Процесс строится по следующим правилам:
1.  **Свободный просмотр:** Ученик может смотреть видео-материалы курса (в рамках своего уровня доступа).
2.  **Линейная практика:** Выполнение практических заданий строго последовательно. Ученик не может отправить задание №2, пока преподаватель не примет и не подтвердит выполнение задания №1.
3.  **Чат как интерфейс прогресса:** Окно чата — это не просто дополнение, а инструмент управления обучением. Именно в нем фиксируются задания, вопросы и системные уведомления об открытии новых этапов.

---

## 🛠 Технологический стек

### Backend (Core)
*   **Runtime:** Node.js (TypeScript)
*   **Framework:** **NestJS** (архитектура модульного монолита)
*   **ORM:** **Prisma 6** (стабильная работа с БД)
*   **Database:** **PostgreSQL**
*   **Real-time:** **Socket.io** (интегрирован через NestJS Gateways)
*   **Security:** **JWT** (авторизация) + **bcryptjs** (хеширование паролей)

### Frontend (UI)
*   **Framework:** **Next.js** (App Router)
*   **Styling:** **CSS Modules** (нативный, чистый CSS для максимального контроля и чистоты кода)
*   **State Management:** React Hooks + Context API
*   **Communication:** Fetch API + Socket.io-client

---

## 👥 Ролевая модель и уровни доступа

В системе предусмотрено три типа учетных записей, что позволяет выстроить эффективную воронку продаж и процесс обучения:

1.  **User (Ознакомительный уровень):** 
    *   Присваивается автоматически при регистрации.
    *   Доступ к вводному курсу («Как пользоваться платформой») и демо-урокам (флаг `isFree` в БД).
    *   Возможность задавать вопросы в чате под доступными уроками.
    *   **Цель:** Ознакомление с интерфейсом и форматом обучения перед покупкой.

2.  **Student (Платный уровень):**
    *   Полный доступ ко всем материалам курса.
    *   Возможность отправлять задания на проверку.
    *   Линейное продвижение по программе обучения.

3.  **Teacher (Уровень управления):**
    *   Доступ к админ-панели управления учениками.
    *   Проверка заданий и предоставление обратной связи в чате.
    *   Управление прогрессом (триггер разблокировки следующего этапа).

---

## 🏗 Архитектура системы

Проект разделен на два независимых, но тесно связанных сервиса:

### 1. Backend (Монолитный сервис) — Порт 4000
Логика разбита на независимые модули внутри одного приложения:
*   **Auth Module:** Регистрация, логин, выдача JWT и проверка прав доступа.
*   **Courses Module:** Управление структурой тем, уроков и контентом (видео, HTML).
*   **Chat Module:** Реал-тайм обработка сообщений и системных уведомлений.

### 2. Frontend (Единый клиент) — Порт 3000
*   Адаптивный интерфейс (Mobile First).
*   Использование **CSS Modules** для каждого компонента, что исключает конфликты стилей и делает HTML-код «чистым».
*   Двухпанельный макет страницы обучения: Контент + Чат.

---

## 🔄 Жизненный цикл обучения (Flow)

1.  **Вход:** Пользователь регистрируется (`User`), смотрит бесплатные уроки.
2.  **Конверсия:** После оплаты роль меняется на `Student`.
3.  **Изучение:** Ученик смотрит видео (внешние хостинги YouTube/Vimeo/Rutube через iframe).
4.  **Действие:** Ученик пишет в чат «Задание выполнил».
5.  **Ревью:** Учитель видит уведомление, проверяет работу, пишет комментарии и нажимает «Принять».
6.  **Прогресс:** 
    *   Бэкенд обновляет статус в БД.
    *   Система автоматически отправляет сообщение `SYSTEM_TASK` с новым заданием.
    *   Через сокеты фронтенд мгновенно разблокирует следующий шаг.

---

## 📁 Структура проекта

```text
/project-root
├── /backend            # Сервер NestJS
│   ├── /src
│   │   ├── /modules    # auth, courses, chat
│   │   ├── /database   # prisma service
│   │   └── main.ts     # Запуск на порту 4000
│   └── /prisma         # Схема и миграции БД
└── /frontend           # Клиент Next.js
    ├── /app            # Страницы и роутинг
    ├── /components     # UI компоненты (CSS Modules)
    └── /lib            # API запросы и сокет-клиент
```

---

## 💡 Особенности реализации

*   **Никакого Tailwind:** Весь дизайн написан на чистом CSS, что гарантирует высокую производительность и легкость кастомизации.
*   **Оптимизация ресурсов:** Сервер не нагружен обработкой видео, занимаясь только логикой и сообщениями.
*   **Модульность:** Несмотря на монолитную структуру, каждый блок (Auth/Chat/Course) автономен и может быть вынесен в микросервис при росте нагрузки.

##  📚 Документация
- [📖 Установка проекта](docs/installation.md)
- [Видео-демонстрация дизайна](https://youtu.be/7HV3K1TWgNU)
- [Архитектура проекта](docs/LinearFlow.md)

Относительно вчерашнего обсуждения системы файлов: её архитектура исходит из пяти базовых операций, которые можно выполнять над файлами.

Если я не упустил ничего важного, то именно на основе этих пяти  действий ( создание, чтение, обновление, удаление, изменение порядка файлов) и будут проектироваться пользовательский интерфейс, его дизайн и фронтенд-реализация.