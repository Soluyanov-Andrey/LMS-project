
---

# 🛠 Git-стандарт и Workflow проекта "Linear Flow"

Данный документ описывает правила ведения разработки, формат коммитов и основные команды для работы с репозиторием.

## 1. Стандарт коммитов (Conventional Commits)

Мы придерживаемся формата:  
`тип(область): описание`

### Типы (Type)
*   **feat**: Добавление новой функциональности.
*   **fix**: Исправление багов.
*   **refactor**: Изменение кода без изменения логики (улучшение читаемости, архитектуры).
*   **style**: Правки верстки, CSS, форматирование (без изменения JS-логики).
*   **docs**: Изменение документации (README, мануалы).
*   **test**: Добавление или изменение тестов.
*   **chore**: Рутинные задачи (обновление зависимостей, изменение конфигов сборки, `.gitignore`).

### Области (Scope)
Область указывает, какую часть монорепозитория затронули изменения:
*   `backend` — общие правки сервера.
*   `frontend` — общие правки клиента.
*   `auth` — логика авторизации.
*   `courses` — контент, уроки, шаги.
*   `chat` — Socket.io и обмен сообщениями.
*   `db` — изменения в Prisma или миграции PostgreSQL.
*   `common` — общие утилиты или типы.

### Примеры
*   `feat(backend): внедрение JWT-авторизации`
*   `style(frontend): адаптация чата под мобильные устройства`
*   `fix(auth): исправлена валидация email в форме регистрации`
*   `chore(db): миграция для добавления поля isFree в таблицу Lesson`

---

## 2. Стратегия ветвления (Branching)

1.  **main**: Всегда стабильный код. Прямые коммиты в `main` запрещены (кроме `docs` или `chore`).
2.  **feature/name**: Ветки для новых фич.
3.  **fix/name**: Ветки для исправления ошибок.

**Рабочий процесс (Workflow):**
1. Создаем ветку от `main`.
2. Делаем работу, коммитим по стандарту.
3. Сливаем (Merge) готовую ветку в `main`.

---

## 3. Шпаргалка по командам Git

### Работа с состоянием
*   `git status` — проверить, какие файлы изменены и что готово к коммиту.
*   `git diff` — посмотреть конкретные изменения в коде.
*   `git log --oneline` — краткая история коммитов.

### Фиксация изменений
*   `git add .` — добавить все изменения в индекс.
*   `git add path/to/file` — добавить конкретный файл.
*   `git commit -m "type(scope): description"` — зафиксировать изменения.

### Работа с ветками
*   `git branch` — список локальных веток.
*   `git checkout -b feature/name` — создать новую ветку и перейти в неё.
*   `git checkout main` — переключиться на основную ветку.
*   `git merge feature/name` — слить изменения из указанной ветки в текущую (находясь в `main`).
*   `git branch -d feature/name` — удалить ветку после слияния.

### Синхронизация с сервером (GitHub)
*   `git push origin branch_name` — отправить ветку на сервер.
*   `git pull origin main` — забрать актуальный код с сервера.
*   `git fetch` — обновить информацию о ветках на сервере без их скачивания.

---

## 4. Пример типового цикла задачи

**Задача: Добавить валидацию пароля на бэкенде.**

```bash
# 1. Переходим в main и забираем свежий код
git checkout main
git pull origin main

# 2. Создаем ветку под задачу
git checkout -b fix(backend)/password-validation

# 3. Делаем правки в коде...

# 4. Проверяем статус и добавляем файлы
git status
git add backend/src/auth/dto/register.dto.ts

# 5. Коммитим по стандарту
git commit -m "fix(auth): добавлена проверка минимальной длины пароля"

# 6. Возвращаемся в main и сливаем ветку
git checkout main
git merge fix(backend)/password-validation

# 7. Отправляем в облако
git push origin main
```

---